<UserControl x:Class="MercatikaApp.Views.OrdersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MercatikaApp.ViewModel"
             xmlns:local="clr-namespace:MercatikaApp.Converters"
             Background="White">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:OrderViewModel />
    </UserControl.DataContext>

    <Grid>
        <!-- Vista Lista -->
        <Grid Visibility="{Binding IsDetailMode, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <DockPanel Margin="5">
                <!-- Botón Recargar arriba del DataGrid -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" DockPanel.Dock="Top" Margin="0,0,0,10">
                    <Button Content="Recharge" Command="{Binding LoadOrdersCommand}" Width="120" Height="30"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding Orders}"
                          SelectedItem="{Binding SelectedOrder}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="Auto"/>
                        <DataGridTextColumn Header="Client" Binding="{Binding ClientId}" Width="Auto"/>
                        <DataGridTextColumn Header="Employee" Binding="{Binding EmployeeId}" Width="Auto"/>
                        <DataGridTextColumn Header="Order date" Binding="{Binding OrderDate, StringFormat=d}" Width="Auto"/>
                        <DataGridTextColumn Header="Address" Binding="{Binding AddressTrip}" Width="200"/>
                        <DataGridTextColumn Header="Province" Binding="{Binding ProvinceTrip}" Width="150"/>
                        <DataGridTextColumn Header="Country" Binding="{Binding CountryTrip}" Width="100"/>
                    </DataGrid.Columns>

                </DataGrid>
            </DockPanel>
        </Grid>

        <!-- Vista Detalle -->
        <Grid Visibility="{Binding IsDetailMode, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Margin="10">
                <TextBlock Text="Order detail" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />
                <TextBlock Text="{Binding SelectedOrder.OrderId, StringFormat=ID: {0}}" />
                <TextBlock Text="{Binding SelectedOrder.ClientId, StringFormat=Cliente: {0}}" />
                <TextBlock Text="{Binding SelectedOrder.EmployeeId, StringFormat=Empleado: {0}}" />
                <TextBlock Text="{Binding SelectedOrder.AddressTrip, StringFormat=Dirección: {0}}" />
                <TextBlock Text="{Binding SelectedOrder.ProvinceTrip, StringFormat=Provincia: {0}}" />
                <TextBlock Text="{Binding SelectedOrder.CountryTrip, StringFormat=País: {0}}" />
                <TextBlock Text="{Binding SelectedOrder.PhoneTrip, StringFormat=Teléfono: {0}}" />
                <TextBlock Text="{Binding SelectedOrder.OrderDate, StringFormat=Fecha Orden: {0:d}}" />
                <TextBlock Text="{Binding SelectedOrder.DateTrip, StringFormat=Fecha Viaje: {0:d}}" />
                <TextBlock Text="Order detail:" FontWeight="Bold" Margin="0,20,0,5" />

                <DataGrid ItemsSource="{Binding OrderDetails}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Height="150">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" />
                        <DataGridTextColumn Header="Product ID" Binding="{Binding ProductDetailId}" />
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" />
                        <DataGridTextColumn Header="Total price" Binding="{Binding LinePrice}" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Edit" Margin="5" Width="80" Height="30"/>
                    <Button Content="Delete" Margin="5" Width="80" Height="30"/>
                    <Button Content="Back" Command="{Binding BackToListCommand}" Margin="5" Width="80" Height="30"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
