<Window x:Class="MercatikaApp.Views.ClientEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Columna 1 -->
                <Label Grid.Row="0" Grid.Column="0" Content="Nombre Empresa:"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CurrentClient.CompanyName}" Margin="5"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Nombre Contacto:"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CurrentClient.ContractName}" Margin="5"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Apellido Contacto:"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentClient.ContractLastname}" Margin="5"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Cargo Contacto:"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CurrentClient.ContractPosition}" Margin="5"/>

                <Label Grid.Row="4" Grid.Column="0" Content="Dirección:"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CurrentClient.Address}" Margin="5"/>

                <!-- Columna 2 -->
                <Label Grid.Row="0" Grid.Column="2" Content="Ciudad:"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding CurrentClient.City}" Margin="5"/>

                <Label Grid.Row="1" Grid.Column="2" Content="Provincia:"/>
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding CurrentClient.Province}" Margin="5"/>

                <Label Grid.Row="2" Grid.Column="2" Content="Código Postal:"/>
                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding CurrentClient.ZipCode}" Margin="5"/>

                <Label Grid.Row="3" Grid.Column="2" Content="País:"/>
                <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding CurrentClient.Country}" Margin="5"/>

                <Label Grid.Row="4" Grid.Column="2" Content="Teléfono:"/>
                <TextBox Grid.Row="4" Grid.Column="3" Text="{Binding CurrentClient.Phone}" Margin="5"/>

                <Label Grid.Row="5" Grid.Column="2" Content="Fax:"/>
                <TextBox Grid.Row="5" Grid.Column="3" Text="{Binding CurrentClient.FaxNumber}" Margin="5"/>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Guardar" Width="100" Margin="5" Command="{Binding SaveCommand}"/>
            <Button Content="Cancelar" Width="100" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>